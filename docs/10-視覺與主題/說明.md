# 10 視覺與主題

介面色在 `src/theme.ts`。改底、墨、對錯、選取時只改那裡，不要在畫面裡另寫一套色票。積木的臉是 `Art/cube` 的圖，對應表在 `src/game/cubeArt.ts`。

## 介面色

底是 `#F6F1E6`（`cream`）。啟動畫面與 Android 圖示底是 `#F6F1E8`（`app.json`），和畫面底差一點，不是同一個常數。

墨色 `#2F2C2A`。頂欄炭色 `#3D3A38`（`charcoal`），上面的字是 `#F6F1E6`。面板白。卡片圓角 18，沒有黑邊，投影是 `softShadow`。強調橙 `#F08A2A`。遊玩圓鈕梅紫 `#6E4E6C`。關卡徽章粉 `#E85A84`。托盤 `#C4B6AE`。正確綠 `#2E7D32`，錯誤紅 `#B00020`，選取金 `#D9B300`。托盤裡已放上棋盤的積木剪影是 `#5C564F`（`spent`）。`progressYellow` 還在色票裡，畫面不再用它畫完成關。`river` 也不拿來上色。首頁膠囊色在 `candy`。

字體不是打包的手寫字。網頁 `Segoe UI`，iOS `Avenir Next`，Android `sans-serif-medium`。漢字若該字型沒有，會退回系統字。根畫面不再載入 Gloria Hallelujah。

背景在 `GridPaper`。選單是菱形，間距 32、邊長 14、色 `diamond`（`#D4C4A8`）。遊玩頁傳 `stripe`，直紋間距 11、色 `stripe`（`#E4D7C4`）。

## 積木與重疊

每一格是 `Art/cube` 裡的一張方塊圖（`cube_001.jpg` 到 `cube_050.jpg`，256×256）。現在有 50 張。同一種覆蓋（同一組積木疊在一起）整區用同一張，玩家才數得出區域有幾格。不同覆蓋不能共用一張，否則會把別區算進去。

`facesForBoard` 在這一盤上分配：單塊先拿自己的圖（托盤上還沒放的、拖曳虛影，跟棋盤上只有它的格子是同一張），重疊再拿還沒用過的圖。圖不夠才重複。已經放上棋盤的托盤格不畫方塊圖，改畫 `spent` 剪影。空格是紙白。

現有關卡的正解，同一盤最多 **26** 種覆蓋（10×10 Extra 第 5 關）。把現有積木換位置亂疊，10×10 量到 **48** 種。50 張蓋得住這兩個數。再加圖要接 `cube_051.jpg`，並在 `src/game/cubeArt.ts` 的 `CUBES` 補上 `require`。

`colors.ts` 裡的舊色票不再用來畫格子。

## 棋盤繪製

遊玩頁把 `Board` 包在白框裡（圓角 16、投影）。`Board` 自己的底是紙白，沒有墨色邊。圓角：格 8、卡片 18、膠囊全圓。線索圓仍用描邊 1.5。

`Board` 每一格：

- 有覆蓋就鋪對應方塊圖，否則紙白。已覆蓋的格各向外多 1px，並把方塊圖裁掉約 6% 的亮邊。圖的亮邊和格縫會像棋盤間線；放上之後不該再看到。空格仍有淡線。
- 選中的格金框，只畫在這塊的外緣。相鄰且同被選中的格之間不畫金線，否則會像棋盤間線。有覆蓋且沒選中時不另加邊。
- `invalidCells` 會把該格透明度降到 0.55。遊玩頁目前固定傳空陣列，所以這條還沒被用到。規則本身不產生這份清單。
- 有線索且那格沒有覆蓋：只畫黑色數字，不墊底。有覆蓋：正圓白紙、黑描邊（`stroke`），數字正確是綠、不正確是紅。

積木預覽元件是 `PieceShape`。

## 修改紀錄

| 日期 | 為什麼改 | 改了什麼 | 方案檢討 |
| --- | --- | --- | --- |
| 2026-09-19 | 建立唯一製作文檔 | 寫下主題色與「重疊用新色、不混合」 | 同日核對：`play.tsx` 的 `invalidCells` 確為 `[]`，已寫明 |
| 2026-09-19 | 視覺對齊 Flow Free Plus | 完成色改黃色下半；卡片圓角 6、描邊 1.5；方格紙改成 28px 直線 | 完成色下半、圓角 6、方格紙 28px／透明度 0.55 仍與程式一致。選關方塊描邊寫在畫面裡是 1.4，不是 `theme.stroke` 的 1.5 |
| 2026-09-19 | 積木改用方塊圖拼，不再平塗 | `PieceShape` 與棋盤有覆蓋的格改畫 `Art/cube`；重疊改挑另一張還沒用過的圖。新增 `src/game/cubeArt.ts` | 方塊圖仍由 `cubeArt` 供給。重疊仍是換圖，不是混色 |
| 2026-09-19 | 放上後不該看見間線；數字要正圓；托盤空位要留著 | 已覆蓋格外擴 1px 並裁亮邊。選中金框只畫外緣。線索沒覆蓋畫黑字，有覆蓋改白圓黑邊、對綠錯紅。托盤已放置改 `spent` 剪影 | 金框外緣、正圓黑邊、托盤剪影仍與程式一致 |
| 2026-09-19 | 不同覆蓋用了同一張方塊圖，格數會算錯 | 改由 `facesForBoard` 在同一盤上分配，圖夠就不重複。寫下正解最多 26 種、亂疊量到 48 種，20 張不夠 | 仍與程式一致。`facesForBoard` 仍負責分配，這次沒改圖 |
| 2026-09-19 | 介面改成參考圖那種軟圓角 | 炭色頂欄、菱形／直紋底、彩色膠囊、深灰選關方塊、粉徽章與梅紫圓鈕、白框棋盤、灰褐托盤。字改系統無襯線，不再載入 Gloria。完成關不再鋪黃下半 | `theme.ts` 的炭色、菱形、直紋、膠囊色、梅紫、粉徽章、`spent` 與系統字仍在。這次沒改那些畫面 |
| 2026-09-20 | 50 張方塊圖已備好，大盤不再因只有 20 張而重複 | `CUBES` 接到 `cube_001.jpg`–`cube_050.jpg` | 待檢討 |
